Einplayer.Backend.Views.TrackList = Backbone.View.extend({

  requiredTemplates: [
    "tracklist"
  ],
  
  initialize: function() {
    this.trackList = this.options.trackList;
  },

  render: function() {
  },
});

